

	/**
	 * Remove the specified resource from storage.
	 *
	 * @param  int  $id
	 * @return Response
	 */
	public function destroy($id)
	{
		${{resource}} = {{model}}::findOrFail($id);
		${{resource}}->delete();

		return ${{resource}};
	}
