

	/**
	 * Display a listing of the resource.
	 *
	 * @return Response
	 */
	public function index()
	{
	    ${{ resource_plural }} = {{ model }}::all();
		return view( '{{ resource_plural }}.index', compact( '{{ resource_plural }}' ) );
	}
